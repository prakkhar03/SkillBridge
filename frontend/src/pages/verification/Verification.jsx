export const Verification = () => {
    return <h1>Verification</h1>
}
// Both login and register component will be rendered in this component, based of user's choice, this component will return the user data.